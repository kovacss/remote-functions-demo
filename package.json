{
  "name": "remote-functions-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build-server": "vite build --ssr src/api.remote.js"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
